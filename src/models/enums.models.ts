export enum LOADING_STATUS {
  IDLE = "IDLE",
  LOADING = "LOADING",
  FAILED = "FAILED",
}

export enum PRODUCT_SORT_ORDER {
  ASC = "asc",
  DESC = "desc",
}

export enum PRODUCT_SORT_BY {
  ID = "id",
  TITLE = "title",
  CATEGORY = "category",
  PRICE = "price",
  RATING = "rating",
}
